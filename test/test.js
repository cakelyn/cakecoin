/*
WHAT TO TEST:
- /getChain should return chain with only genesis block
- /mine should mine a new block and add into the chain
- /transaction/new should add the new transaction to the next mining
 */